from django.apps import AppConfig

class CodepartitionerConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'code_tracer.code_partitioner'  # Full Python path to the application.
